using UnityEngine;

public class ObjectActivator : MonoBehaviour
{
   [SerializeField] RippleEffectReceiver receiver;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
